import { ToTestimoniesPipe } from './to-testimonies.pipe';

describe('ToTestimoniesPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTestimoniesPipe();
    expect(pipe).toBeTruthy();
  });
});
