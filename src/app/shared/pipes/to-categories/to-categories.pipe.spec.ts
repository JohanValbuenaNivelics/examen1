import { ToCategoriesPipe } from './to-categories.pipe';

describe('ToCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
