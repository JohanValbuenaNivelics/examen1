import { ToArticlePymesPipe } from './to-article-pymes.pipe';

describe('ToArticlePymesPipe', () => {
  it('create an instance', () => {
    const pipe = new ToArticlePymesPipe();
    expect(pipe).toBeTruthy();
  });
});
